# Dependencies
node_modules

# Build output
dist
dist-ssr
build
*.local

# Environment
.env
.env.*
!.env.example

# Database (SQLite lives on a volume / local disk, never committed)
backend/data/*.sqlite
backend/data/*.sqlite-shm
backend/data/*.sqlite-wal

# Cloned external reference repos (pulled via `pnpm refs:pull`)
misc/reference/external

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Personal cross-session notes, never shared/pushed
NEXT_SESSION.md
misc/looka-pitch-video.mp4
